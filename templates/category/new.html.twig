{% extends 'base.html.twig' %}

{% block title %}Nouvelle catégorie{% endblock %}

{% block body %}
<h1>Ajouter une catégorie</h1>
{{ form_start(form) }}
{{ form_widget(form) }}
{{ form_end(form) }}
<a href="{{ path('category_index') }}" class="btn btn-secondary mt-2">Retour à la liste</a>
{% endblock %}
